apiVersion: v1
kind: Service
metadata:
  name: doctor-booking-frontend-service
spec:
  selector:
    app: doctor-booking-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
